{
  "name": "longevity-platform-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply longevity-platform-db --local",
    "db:migrate": "wrangler d1 migrations apply longevity-platform-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.16.1"
  }
}
